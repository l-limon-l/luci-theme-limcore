## Bug Fixes

* **modal:** Fix modal container jump/flicker caused by dynamic `overflow` toggling when opening dropdowns inside modal dialogs.
* **layout:** Keep modal and table scrollbars completely stable during dropdown interactions.
