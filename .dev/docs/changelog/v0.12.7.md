# v0.12.7

- Global Table Layout Fix: Applied display: table and display: table-row globally to .table and .tr elements. This fixes a core bug in LuCI div-based tables where cells would shrink to min-content instead of expanding to full width.
- Fixed text wrapping: Replaced buggy wrap-break-word with standard reak-words to prevent aggressive mid-word breaking of network speeds.
