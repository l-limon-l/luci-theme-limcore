## Bug Fixes

* **dropdown:** Fix dropdown animation and remove modal container resizing logic (`max-height: none`) that caused unpleasant flickering when opening dropdown lists inside modals and forms.
* **css:** Restrict `.cbi-dropdown` transitions to color/background properties to prevent position & dimension transition flickering.
